<div id="<%= dom_id budget %>">
  <p>
    <strong>Duration:</strong>
    <%= distance_of_time(budget.duration) %>
  </p>
  <%- budget.errands.each do |errand| %>
    <%= errand %>
  <%- end %>

</div>
